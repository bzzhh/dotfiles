# tmux
alias \
    tn="tmux -u -f ~/.config/tmux/tmux.conf new" \
    ta="tmux -u -f ~/.config/tmux/tmux.conf attach"

# vim
alias \
    vim=nvim \
    magit="nvim -c MagitOnly"

# php
alias \
    sf=symfony

